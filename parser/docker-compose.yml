version: '3.9'

services:
  db:
    extends:
      file: docker-compose.base.yml
      service: db
    ports: [ 5434:5432 ]
  api:
    extends:
      file: docker-compose.base.yml
      service: api
    ports: [ 801:8000 ]

volumes:
  parser_db_volume:

networks:
  parser_db_network:
